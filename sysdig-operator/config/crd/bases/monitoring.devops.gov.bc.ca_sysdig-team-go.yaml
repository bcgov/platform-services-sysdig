---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.16.1
  name: sysdig-team-go.monitoring.devops.gov.bc.ca
spec:
  group: monitoring.devops.gov.bc.ca
  names:
    kind: SysdigTeamGo
    listKind: SysdigTeamGoList
    plural: sysdig-team-go
    singular: sysdigteamgo
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: SysdigTeamGo is the Schema for the sysdig-team-go API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: SysdigTeamGoSpec defines the desired state of SysdigTeamGo
            properties:
              team:
                description: TeamSpec holds the team‐level settings from the CR
                properties:
                  description:
                    type: string
                  users:
                    items:
                      description: UserSpec represents one entry in spec.team.users
                      properties:
                        name:
                          type: string
                        role:
                          type: string
                      required:
                      - name
                      - role
                      type: object
                    type: array
                type: object
            type: object
          status:
            description: SysdigTeamGoStatus defines the observed state of SysdigTeamGo
            properties:
              conditions:
                description: |-
                  INSERT ADDITIONAL STATUS FIELD - define observed state of cluster
                  Important: Run "make" to regenerate code after modifying this file
                items:
                  properties:
                    message:
                      type: string
                    reason:
                      type: string
                    status:
                      type: string
                    type:
                      type: string
                  type: object
                type: array
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
